function atm () {
    while (true) {
        let choice = parseInt(
            prompt(
                "Select a choice:\n1.) See balance\n2.) Make a deposit\n3.) Make a withdrawal\n4.) Get account name\n5.) Exit"
            )
        )
    }
}
atm()